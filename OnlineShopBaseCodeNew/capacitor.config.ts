import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.onlineshopbasecode",
  appName: "OnlineShopBaseCode",
  webDir: "build",
  bundledWebRuntime: false
};

export default config;
