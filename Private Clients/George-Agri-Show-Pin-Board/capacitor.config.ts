import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.gas.pinboard",
  appName: "GeorgeAgriShowPinBord",
  webDir: "build",
  bundledWebRuntime: false
};

export default config;
