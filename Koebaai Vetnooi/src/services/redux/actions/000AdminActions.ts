// import FIREBASE from '../../firebase/FirebaseConfig';

// const fbDatabase = FIREBASE.database();

export const SaveAdimAction = (data: any) => {
	return (dispatch: any) => {
		console.log('recieved data ', data);
	};
};
